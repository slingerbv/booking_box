
<%unless @booking_cities.blank? %>
$('#booking_delivery_address_city').html("<%= escape_javascript(options_for_select(@booking_cities.map {|city| [city.name, city.id]}))%>");
<%else%>
$('#booking_delivery_address_city').html("<%= escape_javascript(options_for_select([]))%>");
<%end%>